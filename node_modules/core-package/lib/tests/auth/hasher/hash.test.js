"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const index_1 = require("../../../src/index");
describe('Hasher', () => {
    describe('checkPassword', () => {
        it('returns true if passwords are equal', () => {
            const password = '$P$PleaseApproveThisMergeRequest';
            const hashedPassword = index_1.hashPassword(password);
            const result = index_1.checkPassword(password, hashedPassword);
            expect(result).toBe(true);
        });
        it('returns false if passwords are different', () => {
            const password = `$P$SeemsGood`;
            const wrongHash = `$P$BLblalblablablalbla`;
            const result = index_1.checkPassword(password, wrongHash);
            expect(result).toBe(false);
        });
    });
    describe('hashPassword', () => {
        it('hashes password', () => {
            const passwordToHash = '$P$SomeGoodPassword';
            const result = index_1.hashPassword(passwordToHash);
            expect(passwordToHash).not.toEqual(result);
        });
    });
});
//# sourceMappingURL=hash.test.js.map