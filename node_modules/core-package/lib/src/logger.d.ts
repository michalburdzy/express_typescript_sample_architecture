import { Logger as PinoLogger } from '@VizMediaEurope/adn-core/node_modules/@types/pino';
import { Request } from '@VizMediaEurope/adn-core/lib/src/node_modules/express';
export interface ILogData {
    message: string;
    context?: any;
}
export interface ILogger {
    debug: ({ message, context }: ILogData) => void;
    error: (error: Error) => void;
    info: ({ message, context }: ILogData) => void;
    trace: ({ message, context }: ILogData) => void;
    warn: ({ message, context }: ILogData) => void;
}
export declare class Logger implements ILogger {
    private serviceName;
    pinoLogger: PinoLogger;
    private request;
    constructor(serviceName: string);
    attachRequest(req: Request): void;
    debug({ message, context }: ILogData): void;
    error(error: Error): void;
    info({ message, context }: ILogData): void;
    trace({ message, context }: ILogData): void;
    warn({ message, context }: ILogData): void;
}
