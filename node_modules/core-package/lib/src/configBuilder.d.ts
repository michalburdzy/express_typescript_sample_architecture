export interface IConfigBuilderOptions<T> {
    requiredVariables?: string[];
    parseFunction: (config: any) => T;
}
export declare const configBuilder: <T>({ requiredVariables, parseFunction, }: IConfigBuilderOptions<T>) => () => T;
