import { Router } from '@VizMediaEurope/adn-core/lib/src/middlewares/node_modules/express';
export interface ILoggerMiddlewareOptions {
    service: string;
}
export declare const attachLoggerMiddleware: ({ service, }: ILoggerMiddlewareOptions) => Router;
