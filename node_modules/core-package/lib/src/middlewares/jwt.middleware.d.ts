import { Request, Response, NextFunction } from '@VizMediaEurope/adn-core/lib/src/middlewares/node_modules/express';
import { IAuthObject } from '@VizMediaEurope/adn-core/lib/src/utils/buildAuthObject';
export { IAuthObject } from '@VizMediaEurope/adn-core/lib/src/utils/buildAuthObject';
export interface RequestWithJwt extends Request {
    jwt: IAuthObject;
}
export interface IJwtMiddlewareOptions {
    isOptional?: boolean;
    requiresAdmin?: boolean;
}
export declare const jwtMiddleware: (options?: IJwtMiddlewareOptions | undefined) => (req: RequestWithJwt, res: Response, next: NextFunction) => void | Response;
