export interface IMailerConfig {
    ACCOUNT_EMAIL: string;
    PUBLIC_KEY: string;
    PRIVATE_KEY: string;
    API_VERSION: string;
    TIMEOUT: number;
}
export declare const buildConfig: () => IMailerConfig;
