import { Router, Express } from '@VizMediaEurope/adn-core/lib/src/testHelpers/node_modules/express';
export interface IBuildAppFunctionArgs {
    prefix: string;
    router: Router;
}
export declare type BuildAppFunction = (args: IBuildAppFunctionArgs) => Express;
export declare const buildTestAppHelper: (buildAppFunction: BuildAppFunction, servicePrefix: string) => (...routers: Router[]) => Express;
