"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.verifyPassword = void 0;
const tsscmp_1 = __importDefault(require("tsscmp"));
const crypto_1 = require("crypto");
const index_1 = require("../index");
const newPasswordStrategy = (password, hash) => index_1.checkPassword(password, hash);
const oldPasswordsStrategy = (password, hash) => {
    const splitedPassword = hash.split(':');
    const salt = splitedPassword[1];
    const hashedPassword = crypto_1.createHash('md5')
        .update(password + salt)
        .digest('hex') + (salt ? `:${salt}` : '');
    return tsscmp_1.default(hashedPassword, hash);
};
exports.verifyPassword = (password, hash) => {
    const result = index_1.isPasswordNew(hash)
        ? newPasswordStrategy(password, hash)
        : oldPasswordsStrategy(password, hash);
    return !!result;
};
//# sourceMappingURL=verifyPassword.js.map