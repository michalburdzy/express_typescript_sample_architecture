export declare enum RequesterMethods {
    GET = "get",
    POST = "post",
    DELETE = "delete",
    HEAD = "head",
    OPTIONS = "options",
    PUT = "put",
    PATCH = "patch"
}
export interface IRequesterOptions {
    url: string;
    method: RequesterMethods;
    data?: any;
    headers?: any;
    params?: any;
    timeout?: number;
}
export declare type Requester = (opt: IRequesterOptions) => Promise<any>;
export declare function requester({ url, method, data, headers, params, timeout, }: IRequesterOptions): Promise<any>;
